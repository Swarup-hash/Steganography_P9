Embedding the Message
To embed the text into the image:

I have using Python steganography library like Stegano or write custom code to modify the least significant bits (LSBs) of the pixel data.

Decoding the Message
To decode the message:

Use the same library or method to extract the LSBs.
This will output the hidden text "Swarup."
